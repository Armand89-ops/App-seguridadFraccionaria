import 'expo-dev-client';
import { registerRootComponent } from 'expo';

import App from './App';

// registerRootComponent se encarga de AppRegistry y compatibilidad con Expo
registerRootComponent(App);
